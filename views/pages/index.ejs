<div ng-app="PowApp">

  <nav id="navbar" class="navbar navbar-default" role="navigation" ng-controller="NavCtrl">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse" ng-click="isCollapsed = !isCollapsed">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><h1>Pow Now</h1></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse" collapse="isCollapsed">
      <!-- <ul class="nav navbar-nav">
        <li><a href="/about">About<i class="glyphicon glyphicon-info-sign"></i></a></li>
      </ul> -->

      <ul class="nav navbar-nav navbar-right">
        <li ng-show="currentUser">
          <a class="user-hello" href="/" ng-show="currentUser">Shred the Gnar, {{currentUser.name || 'Powder Hound'}}</a>
        </li>
        <li><a href="/about"><!-- About --><i class="nav-glyph glyphicon glyphicon-info-sign"></i></a></li>
        <!-- <li ng-hide="currentUser">
          <a href="#" ng-click="showLogin()">Login</a>
        </li>
        <li ng-hide="currentUser">
          <a href="#" ng-click="showSignup()">Sign Up</a>
        </li>
        <li  ng-show="currentUser">
          <a href="#" ng-click="logout()">Logout</a>
        </li> -->
        <li class="dropdown" dropdown>
          <a href="#" class="dropdown-toggle" dropdown-toggle><i class="nav-glyph glyphicon glyphicon-user"></i></a>
          <ul class="dropdown-menu">
            <li ng-hide="currentUser">
              <a href="#" ng-click="showLogin()">Login</a>
            </li>
            <li ng-hide="currentUser">
              <a href="#" ng-click="showSignup()">Sign Up</a>
            </li>
            <li  ng-show="currentUser">
              <a href="#" ng-click="logout()">Logout</a>
            </li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </nav>

  <div class="container">

    <div id="loader" ng-if="$root.loading">
      Loading...
    </div>


    <alerts></alerts>

    <ng-view></ng-view>

  </div>

</div>